[package]
name = "tas2780"
version = "0.1.0"
edition = "2021"

[dependencies]
defmt = "1"
thiserror = { version = "2", default-features = false }

embassy-time = { version = "0.5.0", features = [
    "defmt",
    "defmt-timestamp-uptime",
] }

embedded-hal = "1.0"
